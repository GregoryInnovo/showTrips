import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const UpdateTrip = (props: Props) => {
  return (
    <View>
      <Text>UpdateTrip</Text>
    </View>
  );
};

export default UpdateTrip;

const styles = StyleSheet.create({});
